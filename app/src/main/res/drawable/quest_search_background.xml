<?xml version="1.0" encoding="utf-8"?>
<!-- SearchBar pill: panel fill with a hairline edge so it reads on the slab -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/quest_panel_fill" />
    <stroke android:width="1dp" android:color="@color/quest_slab_edge" />
    <corners android:radius="32dp" />
</shape>
